#!/usr/bin/env bash
# Example script the skill can run.
# Use ${CLAUDE_SKILL_DIR} in SKILL.md to reference it regardless of whether the
# skill is installed at the personal, project, or plugin level.
set -euo pipefail
echo "Generating from template..."
